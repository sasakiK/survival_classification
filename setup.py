from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.0.1',
    description='this project is for analysis to classify survival or not.',
    author='sasaki kensuke',
    license='',
)
